{
  "name": "{{ service_name }}",
  "type": "{{ service_name }}",
  "roles": [
    {
      "name": "spark-client",
      "type": "GATEWAY",
      "hostRef": {
        "hostId": "{{ groups['edge'][0] }}"
      },
      "config": {}
    },
    {
      "name": "spark-history",
      "type": "SPARK_YARN_HISTORY_SERVER",
      "hostRef": {
        "hostId": "{{ groups['slaves'][0] }}"
      },
      "config": {}
    }
  ]
}
